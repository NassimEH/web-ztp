${HTTP_URL} {
  redir ${HTTPS_URL}{uri} permanent
}

${HTTPS_URL} {
   handle_path /static/* {
       root * /web/staticfiles
       file_server
   }

   handle_path /media/* {
       root * /web/media
       file_server
   }

   handle {
       reverse_proxy web:8000
   }

   tls internal
}